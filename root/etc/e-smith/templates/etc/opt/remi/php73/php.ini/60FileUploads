file_uploads                           = On
upload_max_filesize                    = {
    my $mf = $php73{UploadMaxFilesize} || "10";
    $OUT .= "$mf"."M";
}
; added by stephdl see $php73{MaxFileUpload}
max_file_uploads                       = {
    my $mfu = $php73{MaxFileUpload} || "20";
    $OUT .= "$mfu";
}
