max_execution_time                     = {
    my $et = $php72{MaxExecutionTime} || "30";
    $OUT .= "$et";
}
memory_limit                           = {
    my $ml = $php72{MemoryLimit} || "125";
    $OUT .= "$ml"."M";
}
;added by stephdl see $php56{MaxInputTime}
max_input_time                     = {
    my $it = $php72{MaxInputTime} || "60";
    $OUT .= "$it";
}
