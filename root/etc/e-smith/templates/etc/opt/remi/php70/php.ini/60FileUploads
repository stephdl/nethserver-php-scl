file_uploads                           = On
upload_max_filesize                    = {
    my $mf = $php70{UploadMaxFilesize} || "10";
    $OUT .= "$mf"."M";
}
; added by stephdl see $php70{MaxFileUpload}
max_file_uploads                       = {
    my $mfu = $php70{MaxFileUpload} || "20";
    $OUT .= "$mfu";
}
